name: Deploy Modular Site to Firebase Hosting
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      # Step 1: Checks out your repository's code.
      - uses: actions/checkout@v3

      # Step 2: (NEW DEBUGGING STEP) This will print the exact contents
      # of your firebase.json file to the log for us to inspect.
      - name: Display firebase.json content
        run: cat firebase.json

      # Step 3: The deployment action.
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_GALWAY_JAM_CIRCLE_LIVE }}'
          projectId: galway-jam-circle-live
          target: gjc-live
          channelId: live
          
      # Step 4: Writes a summary message.
      - name: Output Deployment URL
        run: |
          echo "### 🚀 Modular Site Deployment Successful! 🚀" >> $GITHUB_STEP_SUMMARY
          echo "**Live Site URL:** https://gjc-live.web.app" >> $GITHUB_STEP_SUMMARY
          echo "It may take a minute for changes to be visible." >> $GITHUB_STEP_SUMMARY
